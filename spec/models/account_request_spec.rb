require 'rails_helper'

RSpec.describe AccountRequest, type: :model do
  # pending "add some examples to (or delete) #{__FILE__}"
  it "deliberately fails for testing PR comments" do
    expect(1).to eq(2)  # This will always fail
  end
end
